#include <iostream>
#include<string>
#include<fstream>

using namespace std;

// program to add two numbers together
int main()
{
	string name[];
    fstream textfile;
    textfile.open("input.txt");

        while(! textfile.eof()){

        }
}
