#include <iostream>
using namespace std;

extern int *readNumbers(int n) ;
extern bool twinArray(int* numbers, int length) ;

int main()
{
	int *arr = readNumbers(6);
	cout<<twinArray(arr,6);
	return 0;
}