#include "Player.h"

int Player::nextID = 1000;

Player::Player(){
    name = "default";
    occupation = "player";
	serviceLength = 0;
    personID=nextID;
	nextID ++;
}

Player::Player(string n, int sl, int *list, int m) : Person(n, sl)
{
	name = n;
	occupation = "player";
	serviceLength = sl;
    personID = nextID;
	nextID ++;
}

int Player::searchGame(int x)
{
	int value;

	return value;
}
int Player::get_salary()
{
	if(serviceLength < 20)
	{
		return salary;
	}
	else
	{
		return (salary*3);
	}
}