#include <iostream>
using namespace std;

extern int *readNumbers(int n) ;
extern void months(int* monthNumbers,int length) ;

int main()
{
	int *arr = readNumbers(10);
	months(arr,10);
	delete[] arr;
	return 0;
}